
type Users @table(name: "users") {
  id: String!
  name: String
  email: String! @unique
  dni: String
  phone: String
  createdAt: Timestamp! @col(name: "created_at", default: "now()")
  updatedAt: Timestamp! @col(name: "updated_at", default: "now()", onUpdate: "now()")
}
