
specVersion: "v1"
serviceId: "comcorp-store-service"
location: "us-central1"
schema:
  source: ["./schema", "./schema/cart.gql"]
  datasource:
    postgresql:
      database: "comcorp-store-database"
      cloudSql:
        instanceId: "comcorp-store-instance"
mutations:
  source: ["./mutations", "./queries/cart.gql"]
queries:
  source: ["./queries", "./queries/products.gql", "./queries/cart.gql"]
connectorDirs: ["./connector"]
