connectorId: "default-2"
# The javascriptSdk generation is removed to prevent build issues.
# generate:
#   javascriptSdk:
#     outputDir: "src/dataconnect/sdk"
#     package: "@firebasegen/default-connector"
