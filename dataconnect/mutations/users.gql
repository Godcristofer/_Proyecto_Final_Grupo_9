mutation CreateUser($id: String!, $email: String!, $name: String) @auth(level: PUBLIC) {
    users_insert(data: {
        id: $id,
        email: $email,
        name: $name,
        createdAt_expr: "request.time"
    })
}
